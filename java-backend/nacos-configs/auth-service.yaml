spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${AUTH_DB_NAME:ai_resume_auth}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: "${DB_USERNAME:root}"
    password: "${DB_PASSWORD:@Feng050813}"
  mail:
    host: smtp.qq.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET:airesumeforgeSecretKeyForJWTTokenGeneration2024VeryLongString}
  expiration: 86400000

email:
  code:
    expiration: 5

gateway:
  service-token: ${GATEWAY_SERVICE_TOKEN:change-me}

oss:
  access-key-id: ${OSS_ACCESS_KEY_ID:}
  access-key-secret: ${OSS_ACCESS_KEY_SECRET:}
  endpoint: ${OSS_ENDPOINT:oss-cn-shenzhen.aliyuncs.com}
  bucket-name: ${OSS_BUCKET:airesume}
  domain: ${OSS_DOMAIN:https://airesume.oss-cn-shenzhen.aliyuncs.com}
  avatar-dir: avatar/
  resume-avatar-dir: resume-avatar/
