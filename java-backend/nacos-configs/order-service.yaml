spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${ORDER_DB_NAME:ai_resume_order}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: "${DB_USERNAME:root}"
    password: "${DB_PASSWORD:@Feng050813}"
  rabbitmq:
    addresses: ${RABBITMQ_URL:amqp://elias:779337@localhost:5672}
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: auto
        default-requeue-rejected: false

gateway:
  service-token: ${GATEWAY_SERVICE_TOKEN:change-me}

alipay:
  app-id: ${ALIPAY_APP_ID:}
  private-key: ${ALIPAY_PRIVATE_KEY:}
  alipay-public-key: ${ALIPAY_PUBLIC_KEY:}
  gateway-url: ${ALIPAY_GATEWAY_URL:https://openapi-sandbox.dl.alipaydev.com/gateway.do}
  notify-url: ${ALIPAY_NOTIFY_URL:http://localhost:8080/api/payment/callback/alipay}
  return-url: ${ALIPAY_RETURN_URL:http://localhost:3000/order/result}
  sign-type: RSA2
